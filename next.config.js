/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Vercelでは特別な設定は不要だが、必要なら以下も
  // env: {
  //   BASE_URL: process.env.BASE_URL
  // }
};

module.exports = nextConfig;

